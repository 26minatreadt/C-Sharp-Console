
class Math //file name
{
    public static void Numbers() { //Numbers Method (this can be called in this script or another) 
        // for intergers only! 
        //int result = 0; //sets the starting int value to 0


        // for math with decimals!
        // double result = 0; //settting the starting double value to 0


        // each math operation | add/subtract/multiply/divide
        //result = 1 + 2; //adding with ints
        //result = 4 - 3; //subtracting with ints
        //result = 12 * 3; //multiplying with ints
        //result = 17 / 3; //dividing with ints

        //writes the result into the log|console
        //Console.WriteLine(result);
    }
    public static void Add(int num1, int num2) { //method for adding two ints
        int result = num1 + num2;
        Console.WriteLine(result);
    }

}